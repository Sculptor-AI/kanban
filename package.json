{
  "name": "kanban",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute kanban-db --file=./migrations/schema.sql",
    "db:migrate:local": "wrangler d1 execute kanban-db --file=./migrations/schema.sql --local"
  },
  "dependencies": {
    "hono": "^4.5.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.57.0"
  }
}
